# Application name (used by Spring Boot for identification)
spring.application.name=app

# Server port (uncomment and set as needed)
# server.port=8080

# MongoDB Atlas connection URI (replace with your own credentials)
spring.data.mongodb.uri=mongodb+srv://<username>:<password>@social.5j8xovc.mongodb.net/?retryWrites=true&w=majority&appName=social

# Optional: Specify a database name
spring.data.mongodb.database=socialDB

# Google OAuth2 configuration
spring.security.oauth2.client.registration.google.client-id=<your-google-client-id>
spring.security.oauth2.client.registration.google.client-secret=<your-google-client-secret>
spring.security.oauth2.client.registration.google.scope=profile,email

# Facebook OAuth2 configuration
spring.security.oauth2.client.registration.facebook.client-id=<your-facebook-client-id>
spring.security.oauth2.client.registration.facebook.client-secret=<your-facebook-client-secret>
spring.security.oauth2.client.registration.facebook.scope=public_profile,email

# Frontend URL (configurable for CORS and redirects)
frontend.url=${FRONTEND_URL:http://localhost:5173}